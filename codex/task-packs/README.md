# Task Packs

These task packs are the Codex workflow layer built from the imported legal managed-agent cookbooks.

Each task pack:

- names the expected inputs
- states connector gates and fallback limits
- defines output artifacts
- keeps the lawyer-review framing explicit

They are hand-authored and intentionally preserved across `scripts/build_codex_port.py` rebuilds.
