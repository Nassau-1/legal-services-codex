# Launch Radar Workers

- `tracker-reader`
  - reads tracker items, comments, and linked context
- `risk-classifier`
  - maps launches to the configured legal calibration
- `memo-writer`
  - owns final write scope for the launch triage memo
