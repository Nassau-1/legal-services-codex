# Coordinators

This directory is reserved for hand-authored coordinator workflows when a legal flow needs a deeper multi-phase Codex orchestration surface than a task pack.

The initial legal port does not add coordinator workflows yet because the imported managed-agent layer maps cleanly into task packs first.
