# Docket Watcher Workers

- `docket-reader`
  - reads new filings and returns structured docket updates
- `deadline-mapper`
  - applies configured rule tables to candidate deadlines
- `tracker-writer`
  - owns final write scope for the report and deadline feed
