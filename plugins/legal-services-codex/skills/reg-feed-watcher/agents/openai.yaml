interface:
  display_name: "Reg Feed Watcher"
  short_description: "Check regulatory feeds now and report what's new since the last check, filtered by your materiality threshold."
  default_prompt: "Use $reg-feed-watcher when you need to check regulatory feeds now and report what's new since the last check, filtered by your materiality threshold."
