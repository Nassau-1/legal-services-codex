---
name: 08-regulatory-legal-reg-feed-watcher
description: Alias skill for `$reg-feed-watcher`. Use this discoverability wrapper when the explicit practice-area-prefixed name is clearer.
---

# Regulatory Legal Feed Watcher

This is a discoverability alias for `$reg-feed-watcher`.

Use it when the explicit practice-area-prefixed entrypoint is clearer than the underlying base skill name.

When invoked:

1. Treat `$reg-feed-watcher` as the canonical underlying skill.
2. Follow the full workflow and constraints from `$reg-feed-watcher`.
3. Preserve the same review framing, source discipline, and connector assumptions as the underlying skill.
