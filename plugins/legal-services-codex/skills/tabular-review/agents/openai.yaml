interface:
  display_name: "Tabular Review"
  short_description: "Tabular review \u2014 one row per document, one column per data point, every cell cited to source."
  default_prompt: "Use $tabular-review when you need to tabular review \u2014 one row per document, one column per data point, every cell cited to source."
