interface:
  display_name: "Draft"
  short_description: "First draft of a common clinic document \u2014 practice-area templates (asylum applications, eviction answers, p..."
  default_prompt: "Use $draft when you need to first draft of a common clinic document \u2014 practice-area templates (asylum applications, eviction answers, p."
