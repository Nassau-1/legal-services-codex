interface:
  display_name: "DSAR Response"
  short_description: "Walk through a Data Subject Access Request (or deletion, portability, correction request) and draft the res..."
  default_prompt: "Use $dsar-response when you need to walk through a Data Subject Access Request (or deletion, portability, correction request) and draft the res."
