---
name: 12-legal-builder-hub-skill-installer
description: Alias skill for `$skill-installer`. Use this discoverability wrapper when the explicit practice-area-prefixed name is clearer.
---

# Legal Builder Hub Skill Installer

This is a discoverability alias for `$skill-installer`.

Use it when the explicit practice-area-prefixed entrypoint is clearer than the underlying base skill name.

When invoked:

1. Treat `$skill-installer` as the canonical underlying skill.
2. Follow the full workflow and constraints from `$skill-installer`.
3. Preserve the same review framing, source discipline, and connector assumptions as the underlying skill.
