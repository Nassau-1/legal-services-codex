# Reg Monitor Workers

- `feed-reader`
  - reads regulatory updates and extracts change summaries
- `materiality-filter`
  - applies configured materiality and policy-gap criteria
- `digest-writer`
  - owns final write scope for the digest and gap summary
